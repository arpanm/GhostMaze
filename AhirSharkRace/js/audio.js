// Minimal audio framework for sea sounds
export const sounds = {
    eat: null,
    hit: null,
    win: null,
    lose: null
};

export function playSound(type) {
    // Placeholder for actual audio implementation
    console.log(`[Sound] Playing: ${type}`);
}
